package com.sort;

import java.util.Arrays;
import java.util.stream.Stream;

public class QuickSort {
    private int[] getLess(int[] arr) {

        return arr;
    }

    private int[] getGreater(int[] arr) {

        return arr;
    }

    public int[] sort(int[] arr) {

        int pivotal = arr[0];
        int[] less = getLess(arr);
        int[] greater = getLess(arr);

//        int[] ar = Stream.concat(Arrays.stream(less), Arrays.stream(greater)).toArray(Integer[]::new);
        return arr;
    }
}
